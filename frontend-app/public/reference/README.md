# Reference Assets

This folder is reserved for parity artifacts that compare the scaffolded app
against the immutable canonical HTML reference in `frontend/TradingHub-Standalone (1).html`.

No generated parity assets have been added yet in this scaffolding step.
